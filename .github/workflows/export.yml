name: Export & Publish

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from pack.toml
        id: version
        run: |
          VERSION=$(grep '^version =' pack.toml | sed 's/.*"\(.*\)".*/\1/')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Determine release tag
        id: tag
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            echo "tag_name=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          else
            echo "tag_name=nightly" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true

      - name: Install packwiz
        run: go install github.com/packwiz/packwiz@latest

      - name: Download mods to cache
        run: packwiz update --all --yes

      - name: Add CurseForge metadata (CFWidget, no key needed)
        run: python3 add-cf-metadata.py

      - name: Export to CurseForge
        run: packwiz cf export -o CreateVC-Light.zip

      - name: Export to Modrinth
        run: packwiz mr export -o CreateVC-Light.mrpack

      - name: Upload to Modrinth
        if: startsWith(github.ref, 'refs/tags/')
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          curl -X POST https://api.modrinth.com/v2/version \
            -H "Authorization: $MODRINTH_TOKEN" \
            -F "data={\"name\":\"$VERSION\",\"version_number\":\"$VERSION\",\"dependencies\":[],\"game_versions\":[\"1.21.1\"],\"version_type\":\"release\",\"loaders\":[\"neoforge\"],\"featured\":true,\"status\":\"listed\",\"project_id\":\"createvc-server\",\"file_parts\":[\"file\"]};type=application/json" \
            -F "file=@CreateVC-Light.mrpack"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CreateVC-Light-Exports
          path: |
            CreateVC-Light.mrpack
            CreateVC-Light.zip
          compression-level: 0

      - name: Create/update release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.tag.outputs.tag_name }}
          name: ${{ steps.tag.outputs.tag_name == 'nightly' && 'Nightly Build' || steps.version.outputs.version }}
          prerelease: ${{ steps.tag.outputs.tag_name == 'nightly' }}
          allowUpdates: true
          replacesArtifacts: true
          artifacts: "CreateVC-Light.zip,CreateVC-Light.mrpack"
          token: ${{ secrets.GITHUB_TOKEN }}
