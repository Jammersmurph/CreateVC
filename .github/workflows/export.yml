name: Export & Publish

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from pack.toml
        id: version
        run: |
          VERSION=$(grep '^version =' pack.toml | sed 's/.*"\(.*\)".*/\1/')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Determine release tag
        id: tag
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            echo "tag_name=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          else
            echo "tag_name=Modpacks" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true

      - name: Install packwiz
        run: go install github.com/packwiz/packwiz@latest

      - name: Download mods to cache
        run: packwiz update --all --yes

      - name: Add CurseForge metadata (CFWidget, no key needed)
        run: python3 add-cf-metadata.py

      - name: Export to CurseForge
        run: packwiz cf export -o CreateVC.zip

      - name: Exclude FTB Library from Modrinth export
        run: mv mods/ftb-library-forge.pw.toml /tmp/

      - name: Export to Modrinth
        run: packwiz mr export -o CreateVC.mrpack

      - name: Upload to Modrinth
        continue-on-error: true
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          VERSION_ID=$( \
            curl -s -X POST https://api.modrinth.com/v2/version \
              -H "Authorization: $MODRINTH_TOKEN" \
              -F "data={\"name\":\"$VERSION\",\"version_number\":\"$VERSION\",\"dependencies\":[],\"game_versions\":[\"1.21.1\"],\"version_type\":\"release\",\"loaders\":[\"neoforge\"],\"featured\":true,\"status\":\"listed\",\"project_id\":\"BpnBfkpK\",\"file_parts\":[\"file\"]};type=application/json" \
              -F "file=@CreateVC.mrpack" \
            | python3 -c "import json,sys; print(json.load(sys.stdin).get('id',''))" \
          )
          if [ -n "$VERSION_ID" ]; then
            curl -s -X PATCH "https://api.modrinth.com/v2/project/BpnBfkpK" \
              -H "Authorization: $MODRINTH_TOKEN" \
              -H "Content-Type: application/json" \
              -d "{\"server_details\": {\"modpack_version_id\": \"$VERSION_ID\"}}"
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CreateVC-Exports
          path: |
            CreateVC.mrpack
            CreateVC.zip
          compression-level: 0

      - name: Update Modpacks release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.tag.outputs.tag_name }}
          allowUpdates: true
          omitBody: true
          omitName: true
          omitPrereleaseDuringUpdate: true
          removeArtifacts: true
          artifacts: "CreateVC.zip,CreateVC.mrpack"
          token: ${{ secrets.GITHUB_TOKEN }}
